# Optional — when set, "Schedule Walkthrough" (summary) uses tel:; escalation uses Call +17743579384 in UI regardless.
# use a real <a href="tel:…"> (works in Chrome/Safari/Firefox). Embedded dev browsers may ignore tel:.
# E.164, e.g. +15551234567 (spaces ok, they are stripped). Restart npm run dev after changing.
# VITE_SPECIALIST_PHONE=+15551234567
